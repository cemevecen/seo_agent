# Mac bridge worker — Playwright Firefox + Selenium (sistem Firefox.app).
# Kurulum: pip install -r requirements.txt && pip install -r requirements-mac.txt
# (Darwin'de requirements.txt zaten marker ile kurar; bu dosya Linux CI/acil için açık liste.)
-r requirements.txt
playwright==1.50.0
selenium>=4.18.0
