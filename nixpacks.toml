# Railway Nixpacks: Playwright/Firefox/Selenium KURULMAZ (requirements Darwin marker).
# Browser scrape yalnız Mac bridge — bulutta RAM/disk faturası yok.
[phases.build]
dependsOn = ["install"]
cmds = ["echo 'nixpacks: playwright/firefox skipped (Mac bridge only)'"]
